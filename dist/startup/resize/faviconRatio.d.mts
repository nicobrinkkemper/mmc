/**
 * Calculates the aspect ratio of an image, which can be used to create a favicon.
 */
export declare function aspectRatio({ originalWidth, originalHeight, maxWidth, maxHeight, }: {
    originalWidth: number;
    originalHeight: number;
    maxWidth?: number;
    maxHeight?: number;
}): {
    width: number;
    height: number;
};
//# sourceMappingURL=faviconRatio.d.mts.map